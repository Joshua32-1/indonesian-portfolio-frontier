{
  "name": "idx-portfolio-workbench",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Unified deployable site: the optimizer and the backtester over one editable ticker universe, fed live by the root /api functions.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.2"
  }
}
